#include "main.h"
/**
 *_islower - prototype used
 *@c: character to be checked
 *Return: 1 when condition is true, 0 false
 */
int _islower(int c)
{
if (c >= 97 && c <= 122)
{
return (1);
}
return (0);
}
